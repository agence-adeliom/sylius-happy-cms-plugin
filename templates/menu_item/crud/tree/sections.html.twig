<div class="page-body">
    <div class="container-xl">
        <div class="row">
            <div class="col-12 col-lg-4">
                {% set menuId = (app.request.attributes.get('id') ?? app.request.query.get('id'))|default(null) %}
                {% hook 'sections#left' %}
            </div>
            <div class="col-12 col-lg-8">
                <div class="page-body">
                    <div class="container-xl">
                        {% hook 'sections#right' %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
